name: One-time stale branch cleanup

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  delete-stale-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Delete cleanup/repo-boundaries-v3
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          status="$({
            curl --silent --show-error --location \
              --output /tmp/delete-ref-response \
              --write-out '%{http_code}' \
              --request DELETE \
              --header "Authorization: Bearer ${GH_TOKEN}" \
              --header 'Accept: application/vnd.github+json' \
              --header 'X-GitHub-Api-Version: 2022-11-28' \
              "https://api.github.com/repos/${GH_REPO}/git/refs/heads/cleanup/repo-boundaries-v3"
          } || true)"

          if [[ "${status}" == "204" ]]; then
            echo "Deleted cleanup/repo-boundaries-v3"
          elif [[ "${status}" == "422" || "${status}" == "404" ]]; then
            echo "cleanup/repo-boundaries-v3 is already absent"
          else
            echo "Failed to delete cleanup/repo-boundaries-v3: HTTP ${status}" >&2
            cat /tmp/delete-ref-response >&2 || true
            exit 1
          fi
